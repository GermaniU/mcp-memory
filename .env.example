# ─── Almacenamiento (SQLite + FTS5, sin infra externa) ────────────────
# Archivo local. "~" se expande. ":memory:" para efímero (tests).
DB_PATH=~/.agent-memory/memory.db

# ─── MCP server ─────────────────────────────────────────────────────
MCP_PORT=8765
DEFAULT_NAMESPACE=default
